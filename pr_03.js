function sumNumbers(a, b) {
  console.log(result); // Logging the variable before it is assigned a value
  var result = a + b;
  return result;
}

console.log(sumNumbers(5, 7)); // Calling the function
